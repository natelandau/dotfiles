{{- if lookPath "claude" }}
# Gradual rollout of memory usage is underway. Force it to be enabled. 2026-02-23
export CLAUDE_CODE_DISABLE_AUTO_MEMORY=0

alias cc="claude"
alias ccd="claude --dangerously-skip-permissions"
alias cch="claude --chrome"

{{- end }}
